apiVersion: v1
kind: Service
metadata:
  name: protostar-nest-service
  namespace: service
  labels:
    app: protostar
    tier: backend
spec:
  selector:
    app: protostar
    tier: backend
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP